const canvasWidth = 800,
      canvasHeight = 450,
      noteWidth = 24,
      noteHeight = 16,
      noteRadius = 2,
      noteOffset = 0.1,
      canvasScale = 20,
      keyCount = 8,
      trackWidth = noteWidth * keyCount;

let speed = 12,
    volume = 50;

let pause = false;

